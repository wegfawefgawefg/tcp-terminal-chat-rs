# tcp-terminal-chat-rs
A tiny educational Rust project: a TCP broadcast server (webservi) plus a terminal UI client (webclient) for simple chat over localhost. Built to learn networking, threads, and TUI event loops; not production-ready.
